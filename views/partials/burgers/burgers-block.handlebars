<li class ="burger-list">
    {{bname}}

    <button class="change-devour btn-success" data-id="{{id}}" 
    data-burgerstate="{{devoured}}">
    {{#if devoured}} Devoured {{else}} Not Devoured {{/if}}
    </button>

    
</li>


